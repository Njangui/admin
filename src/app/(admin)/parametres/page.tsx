import type { Metadata } from 'next'
import { ParametresPage } from '@/components/settings/ParametresPage'
export const metadata: Metadata = { title: 'Paramètres' }
export default function Page() { return <ParametresPage /> }
